@model ProductType

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .edit-type-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    .edit-type-box {
        width: 100%;
        max-width: 400px;
        padding: 40px;
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
    }

    .form-control, .btn {
        border-radius: 50px;
    }

    .form-group > label {
        padding-left: 20px;
    }

    .btn-primary {
        background-color: #ff9800;
        border: none;
    }

    .btn-primary:hover {
        background-color: #e68a00;
    }

    h1, h4 {
        font-weight: bold;
        color: #ff9800;
    }

    .back-link {
        color: #ff9800;
        text-decoration: none;
    }

    .back-link:hover {
        color: #e68a00;
        text-decoration: underline;
    }
</style>

<div class="edit-type-container">
    <div class="edit-type-box">
        <h1 class="text-center">Edit</h1>
        <h4 class="text-center">Product Type</h4>
        <hr />
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input name="TypeID" type="hidden" value="@Model.TypeID" />

            <div class="form-group mb-3">
                <label asp-for="TypeName" class="control-label">Product Type Name</label>
                <input asp-for="TypeName" class="form-control" />
                <span asp-validation-for="TypeName" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <input type="submit" value="Save" class="w-100 btn btn-lg btn-primary" />
            </div>
        </form>

        <div class="text-center mt-3">
            <a asp-action="Index" class="back-link">Back to List</a>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
